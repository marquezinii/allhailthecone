---
import Base from "../layouts/Base.astro";
import PageHeading from "../components/PageHeading.astro";
import Art from "../components/Art.astro";
import { territories } from "../data/universe";
import { Content } from "../../WORLD.md";
---

<Base
  title="The world"
  description="Explore Higher Ground: The First Ground, Crown City, floating realms, deep cities and the unknown Gates beyond."
>
  <PageHeading
    title="The map is still unfolding."
    label="A traveller’s atlas"
    description="A fragmented universe connected by Gates, maintained routes and one direction whose destination remains unknown."
  />
  <div class="wrap">
    <div class="world-atlas-image">
      <Art id="the-kindness-drift" eager sizes="90vw" />
      <span class="image-tag">The Floating Realms · Visual field study</span>
    </div>
    <div class="territory-grid">
      {territories.map((t) => (
        <article class="territory" id={t.id}>
          <p class="eyebrow">{t.kind}</p>
          <h2>{t.name}</h2>
          <p>{t.text}</p>
        </article>
      ))}
    </div>
  </div>
  <article class="wrap prose world-dossier">
    <Content />
  </article>
</Base>
