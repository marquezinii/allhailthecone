---
import Art from "./Art.astro";
import SacredConeExperience from "./experience/SacredConeExperience";
import { getLocale } from "../i18n";

interface Props {
  compact?: boolean;
  immersive?: boolean;
}

const { compact = false, immersive = false } = Astro.props;
const locale = getLocale(Astro.url.pathname);
const copy = {
  en: ["ORIGIN UNRECORDED", "STATUS", "STILL HERE"],
  pt: ["ORIGEM NÃO REGISTRADA", "STATUS", "AINDA AQUI"],
  zh: ["起源未记录", "状态", "仍在此处"],
  de: ["URSPRUNG NICHT VERZEICHNET", "STATUS", "NOCH HIER"],
  fr: ["ORIGINE NON CONSIGNÉE", "STATUT", "TOUJOURS ICI"],
}[locale];
---

<div class:list={["relic", { compact, immersive }]} data-depth>
  <div class="relic-stars" aria-hidden="true"></div>
  <div class="relic-space" aria-hidden="true">
    <div class="relic-orbit orbit-one"></div>
    <div class="relic-orbit orbit-two"></div>
    <div class="relic-axis"></div>
    <div class="relic-object">
      <Art
        id="the-sacred-signal"
        decorative
        sizes="(max-width: 760px) 290px, 360px"
      />
    </div>
    <div class="relic-floor"></div>
    <div class="relic-plinth">
      <span></span>
    </div>
    <div class="relic-dust dust-one"></div>
    <div class="relic-dust dust-two"></div>
    <div class="relic-dust dust-three"></div>
  </div>
  {immersive && (
    <SacredConeExperience client:visible={{ rootMargin: "240px" }} />
  )}
  <div class="relic-coordinate coord-left" aria-hidden="true">
    OBJECT C–001
    <br />
    <span>{copy[0]}</span>
  </div>
  <div class="relic-coordinate coord-right" aria-hidden="true">
    {copy[1]}
    <br />
    <span>{copy[2]}</span>
  </div>
</div>
